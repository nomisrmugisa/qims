import React from 'react';
import { Typography } from '@mui/material';

export default function RolesPage() {
  return (
    <div>
      <Typography variant="h4">Roles Page</Typography>
    </div>
  );
}