import React from 'react';
import { Typography } from '@mui/material';

export default function RequestsPage() {
  return (
    <div>
      <Typography variant="h4">Requests Page</Typography>
    </div>
  );
}
